完全赞同！使用路由守卫（Route Guard）是 React 应用中处理权限的标准做法，能彻底杜绝“首页闪现”的问题，体验更专业。

我将立即执行以下优化方案：

1.  **创建路由守卫组件**
    *   新建 `src/components/RequireAuth.tsx`。
    *   逻辑：检查 `useAuthStore` 中的 `user`。如果为空，直接返回 `<Navigate to="/login" replace />`；如果不为空，返回 `{children}`。

2.  **修改路由配置 (App.tsx)**
    *   引入 `RequireAuth`。
    *   将 `HomePage` 和 `CalendarPage` 都包裹在 `RequireAuth` 中。这样用户必须登录才能访问任何功能页面。

3.  **优化登录页 (LoginPage.tsx)**
    *   移除左上角的返回箭头，使其成为唯一的入口。
    *   （可选）如果用户已登录但访问 `/login`，自动跳回首页（防止重复登录）。

我将立即开始编写代码。