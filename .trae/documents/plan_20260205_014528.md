收到，立即开始实施“手术记录本回收站”功能。

### 🛠️ 执行步骤

1.  **数据层升级 (Types & Store)**
    *   **更新类型**：在 `src/types/index.ts` 中为 `SurgeryRecord` 添加 `deletedAt` 字段。
    *   **改造 Store**：修改 `src/store/useSurgeryStore.ts`。
        *   `deleteRecord`：改为软删除（设置 `deletedAt`）。
        *   新增 `restoreRecord`：恢复记录（清除 `deletedAt`）。
        *   新增 `permanentDeleteRecord`：物理删除。
        *   新增 `cleanupTrash`：清理 7 天前的记录。
        *   **关键修正**：更新 `records` 的获取逻辑，确保 `SurgeryHistoryPage` 只显示未删除的记录。

2.  **页面层开发 (Pages)**
    *   **新建页面**：创建 `src/pages/SurgeryTrashPage.tsx`，复用 `TrashPage` 的 UI 逻辑，适配手术记录的字段。
    *   **路由注册**：在 `src/App.tsx` 中添加 `/surgery-trash` 路由。
    *   **入口集成**：在 `SurgeryHistoryPage` 的顶部添加回收站图标入口。

我将从更新类型定义开始。