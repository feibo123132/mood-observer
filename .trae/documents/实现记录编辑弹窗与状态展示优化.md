这是一个非常好的交互优化，让数据不仅是“死”的记录，而是可以互动的、可修正的记忆。

我将执行以下方案：

## 1. 增强数据展示 (CalendarPage & ReviewPage)
*   **状态显示**：在分数（如 50）旁边，增加对应的状态文本（如“平静 / 归零”）。这需要调用 `getMoodState(score).label`。

## 2. 实现编辑/详情弹窗 (EditModal)
*   **交互逻辑**：点击任意一条记录卡片，弹出一个 Modal（模态框）。
*   **功能**：
    *   **展示详情**：显示该记录的时间、分数、状态。
    *   **修改笔记**：提供一个文本域 (`textarea`)，允许用户修改 `note` 内容。
    *   **保存**：点击保存后，调用 `useMoodStore.updateRecord`（需要新增此方法）更新本地和云端数据。
    *   **评论/备注**：目前您的需求是“文字内容的修改以及进一步评论”，这其实可以统一理解为**“追加或修改 Note 字段”**。为了简单有效，我将允许用户直接编辑 Note 内容。如果您希望评论是独立的字段，我们需要修改数据结构，建议先直接复用 Note 字段，让用户可以在原有基础上追加内容。

## 3. Store 升级
*   在 `useMoodStore` 中新增 `updateRecord(id, newNote)` 方法，支持更新记录的文字内容。

## 执行步骤
1.  修改 `useMoodStore.ts`：添加 `updateRecord` 方法。
2.  创建 `RecordEditModal` 组件：用于展示详情和编辑。
3.  修改 `CalendarPage.tsx` 和 `ReviewPage.tsx`：接入弹窗逻辑，并优化列表项展示（增加状态文本）。

我将先从升级 Store 开始。