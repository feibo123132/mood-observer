好的，立即执行！我们将把“回收站”和“同步”按钮收纳进一个优雅的下拉菜单中。

### 🛠️ 执行步骤

1.  **修改 `src/pages/SurgeryHistoryPage.tsx`**
    *   **引入图标**：引入 `Menu` (汉堡菜单) 图标。
    *   **新增状态**：添加 `isMenuOpen` 状态来控制菜单的显示与隐藏。
    *   **重构头部**：
        *   移除原来的两个独立按钮。
        *   添加一个新的菜单触发按钮。
        *   实现下拉菜单 UI（绝对定位，包含“回收站”和“同步”两个选项）。
        *   添加点击外部关闭菜单的逻辑（可选，为了简单起见，点击菜单项后自动关闭即可）。

我将直接修改代码，实现这个交互优化。