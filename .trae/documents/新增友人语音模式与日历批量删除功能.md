收到！这将进一步丰富应用的个性化体验和实用性。

我将分为三个步骤来执行：

## 1. 核心逻辑升级 (Store & Resources)
*   **SettingsStore**: 更新音频模式定义，增加 `'friend'` (友人模式)。
*   **MoodStore**: 新增 `deleteMultipleRecords(ids)` 方法，支持批量删除，确保本地和云端同步执行。
*   **资源准备**: 自动创建 `public/audio/friend` 目录及占位文件。

## 2. 界面更新：友人模式 (UserMenu)
*   在“语音效果”菜单中，追加“友人模式”选项。

## 3. 界面更新：批量删除 (CalendarPage)
*   **交互设计**：
    *   在日历标题旁增加一个“多选”按钮。
    *   进入多选模式后，每条记录左侧滑出复选框。
    *   底部悬浮出现操作栏：“删除选中 (X)”，点击后进行二次确认并批量删除。
    *   支持“全选/取消全选”。

我将先从底层数据逻辑和资源准备开始。