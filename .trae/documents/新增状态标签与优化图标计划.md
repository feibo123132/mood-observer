您有两个需求：
1.  **优化图标**：将“情绪魔法师JIEYOU”左侧的设置（齿轮）图标更换为更匹配“魔法”主题的图标（如 `Wand2`）。
2.  **新增状态标签功能**：在心情球体（`MoodCard`）左上角添加一个标签入口，点击后弹出标签选择器，支持多种活动/情绪标签。

### 🛠️ 优化计划

#### 1. 替换魔法师图标 (`src/components/AIReportCard.tsx`)
*   **目标**: 将 `Settings` 图标替换为 `Wand2`（魔杖），与弹窗内的图标保持一致。

#### 2. 实现标签选择功能
这是一项较大的功能更新，涉及 UI、数据存储和类型定义。

*   **A. 更新数据结构 (`src/types.ts` & `src/store/useMoodStore.ts`)**
    *   在 `MoodRecord` 类型中新增 `tags: string[]` 字段（虽然您只说“一个标签”，但为了未来扩展，设计为数组更佳，或者暂时只用 `tag?: string`，根据您的描述“点击后会弹出一个的弹窗...标签类型依次有...”，这通常意味着给当前记录打标签）。
    *   **重要确认**: 这些标签是打在**当前正在记录**的那条心情上吗？是的，逻辑上应该如此。
    *   **Store 更新**: `addRecord` 方法需要支持传入 `tag`。

*   **B. 创建标签选择弹窗 (`src/components/TagSelectionModal.tsx`)**
    *   **UI**: 一个网格布局的弹窗，展示您列出的所有标签。
    *   **标签列表**: “游戏”、“吃吃吃”...“裂开”等。
    *   **交互**: 点击某个标签 -> 选中并关闭弹窗 -> 将标签回传给父组件。

*   **C. 修改 MoodCard 组件 (`src/components/MoodCard.tsx`)**
    *   **入口**: 在球体左上角添加一个圆形小按钮（或胶囊按钮）。
        *   如果已选标签：显示标签名 + 图标。
        *   未选标签：显示“#”或标签图标。
    *   **状态**: 新增 `selectedTag` 状态。
    *   **提交逻辑**: 当用户拖动滑块完成记录时（或者点击完成时），将 `selectedTag` 一并保存到记录中。
        *   *注意*: 目前 `MoodCard` 主要是展示当前分数和滑动条。记录的保存逻辑在哪里？通常是在 `MoodInput` 或主页面中。检查代码发现 `MoodCard` 似乎只负责展示球体和滑动条。
        *   **更正**: 查看 `MoodCard.tsx`（虽然我还没读到，但我可以推测），它可能包含滑动条。如果不包含，那我需要找到包含滑动条和“记录”按钮的组件。根据截图，`MoodCard` 应该就是那个球体+滑块的组件。
        *   **关键点**: 您希望在“球体的左上角”加标签。这意味着这个标签是**本次记录**的一部分。

### 📅 执行顺序
1.  **修改 AIReportCard**: 替换图标。
2.  **定义标签常量**: 在 `src/constants/tags.ts` (新建) 中定义所有标签。
3.  **新建 TagSelectionModal**: 实现标签选择 UI。
4.  **修改 MoodCard**:
    *   引入标签选择逻辑。
    *   在左上角添加标签按钮。
    *   **数据对接**: 确保标签能被保存。

**待确认点**:
*   `MoodCard` 是否负责保存数据？如果是，我需要在 `handleSave` 时带上标签。
*   如果 `MoodCard` 只是纯展示，数据保存在父组件，那我需要通过回调把标签传出去。
*   *由于我之前没读到 `MoodCard.tsx`（文件不存在？可能是路径不对或文件名不对），我需要先找到正确的文件。*
*   根据截图，球体和滑块在一起。通常这个组件叫 `MoodSphere` 或者就在 `HomePage` 里直接写了？
*   **搜索**: 我将先搜索一下包含 "SCORE:" 或 "开心 / 愉悦" 的文件，定位代码位置。

请确认执行。