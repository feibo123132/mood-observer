我将创建 `src/pages/TrashPage.tsx` 来修复页面缺失导致的报错。

### 📄 实现细节
1.  **数据过滤安全策略**:
    *   用户提醒需要筛选 `userId === currentUser.email`。
    *   **现状分析**: 经查阅代码，本地 Store 中的 `MoodRecord` 对象**不包含** `userId` 字段（该字段仅在云端存储和同步时使用）。
    *   **解决方案**: 由于 `useMoodStore` 在拉取数据时已经严格执行了 `where({ userId: currentEmail })`，因此 Store 中的数据天然是安全的。我将在代码中注释说明这一点，并重点过滤 `deletedAt !== null` 的记录。
2.  **UI 设计**:
    *   **头部**: 简洁的导航栏，带返回按钮。
    *   **列表**: 展示被删除记录的日期、心情分数（带颜色标识）和笔记预览。
    *   **操作**: 每条记录提供 "♻️ 恢复" 和 "🗑️ 彻底删除" 两个操作按钮。
    *   **空状态**: 当没有被删除的记录时，显示友好的空状态提示。
3.  **类型处理**:
    *   由于 `MoodRecord` 类型定义中缺失 `deletedAt`，我将在页面内部临时扩展该类型以避免 TypeScript 报错。

### ✅ 预期效果
*   应用不再报错，`TrashPage` 路由可正常访问。
*   用户可以查看并管理已删除的情绪记录。

请确认执行。