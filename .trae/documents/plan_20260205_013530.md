好的，我们将采用 **“详情弹窗 + 长表单快捷编辑”** 的方案。

### 核心任务清单

1.  **Store 层升级 (`useSurgeryStore.ts`)**
    *   检查并补充 `deleteRecord`（删除记录）和 `updateRecord`（更新记录）方法，确保数据能持久化到云端。

2.  **组件层开发 (`SurgeryDetailModal.tsx`)**
    *   **新建组件**：创建一个全屏/半屏弹窗组件。
    *   **展示模式**：优雅地展示 8 个步骤的完整内容，使用卡片和图标区分不同阶段。
    *   **编辑模式**：点击“编辑”后，文本区域变为 `textarea`，允许用户直接修改内容。
    *   **操作栏**：顶部包含“关闭”、“删除”、“编辑/保存”按钮。

3.  **页面层集成 (`SurgeryHistoryPage.tsx`)**
    *   **交互接入**：在记录列表中，点击某条记录时打开 `SurgeryDetailModal`。
    *   **数据流转**：将选中的记录传递给弹窗，并处理更新和删除的回调。

我将从 Store 层开始，确保地基稳固。