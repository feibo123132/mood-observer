好的，这四个需求都非常明确，我将按以下方案执行：

## 1. 极致丝滑的色彩渐变 (Gradient Mood)
*   **重构算法**：目前 `moodUtils.ts` 是“阶梯式”返回颜色的。我将引入**线性插值算法**。
*   **实现逻辑**：
    *   定义 11 个关键帧颜色点（Anchor Colors）。
    *   当分数处于两个关键帧之间时（例如 45 分，在 40 和 50 之间），算法会自动计算出中间色值。
    *   修改 `MoodSphere` 和 `MoodSlider` 组件，让球体颜色、滑块拖拽球颜色都使用这个动态计算出的渐变色，实现“每动一分，颜色即变”。

## 2. 数据后悔药：回收站 (Trash Bin)
*   **数据结构升级**：在记录中增加 `deletedAt` 字段，标记删除时间。
*   **软删除机制**：
    *   修改 `deleteRecord`：不再直接抹除数据，而是打上“已删除”标记。
    *   修改 `Calendar/Review` 页面：过滤掉已删除的记录，确保它们在主界面“消失”。
*   **回收站页面**：
    *   新增 `/trash` 页面。
    *   展示被删除的记录，显示“剩余天数”（7天倒计时）。
    *   提供“恢复”和“永久删除”功能。
*   **自动清理**：在应用启动时检查过期（>7天）的垃圾，并永久清除。

## 3. UI 细节优化
*   **多选图标移位**：将 `CalendarPage` 顶部的多选图标下移至日期子标题行（例如“2026年 1月 12日”右侧）。
*   **日期格式化**：将所有中文数字格式（“一月”）改为阿拉伯数字格式（“1月”），统一为 `yyyy年M月d日`。

我将先从色彩算法和数据结构入手。