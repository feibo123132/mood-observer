我将为您修改 `CalendarPage.tsx` 中的日历颜色逻辑，以确保当天显示的颜色始终对应当天的**最高分**，无论这个分数是来自“情绪之森”还是“宝藏之旅”。

### 修改计划

1.  **移除数据筛选限制**：
    在 `dailyStats` 的计算逻辑中，目前有一行代码 `if (r.type === 'harvest') return;` 明确跳过了“宝藏之旅”的记录。我将移除这一行，确保所有类型的记录都参与统计。

2.  **统一颜色获取逻辑**：
    目前代码只使用 `getMoodState` 来获取颜色。虽然 `getHarvestLevel` 也有颜色定义，但为了保持日历视觉的一致性（通常是基于分数的颜色梯度），直接使用 `getMoodState` 根据最高分获取颜色是合理的，因为两者在相同分数段的颜色定义是兼容的（例如高分都是金色/亮色）。

3.  **确认最高分逻辑**：
    现有的 `scoreToUse` 逻辑已经包含了“取最大值”的判断（`stat.min >= 50 ? stat.max : ...`）。只要我们将“宝藏之旅”的数据纳入统计，且这天没有低于 50 分的记录，系统就会自动取包括“宝藏之旅”在内的所有记录中的最高分。

    *微调建议*：为了严格符合您的要求“显示当天最高分时对应的颜色”，我可以简化 `scoreToUse` 的逻辑，直接取 `stat.max`，或者保留现有的“低分加权”逻辑（如果那天有很低的分数，可能不希望显示为开心）。根据您的描述“不管是情绪之森还是宝藏之旅”，您似乎更倾向于**只要有高分就显示高分颜色**，或者至少是这两类记录混合后的最高分。
    
    *决定*：我将修改 `dailyStats` 统计逻辑，不再过滤 `harvest` 类型。同时，为了确保“宝藏之旅”的高分（通常较高）能决定颜色，现有的逻辑（如果全天无低分则取最高分）应该能很好地工作。如果那天既有 96 分的宝藏之旅，又有 75 分的情绪之森，且没有低于 50 分的记录，系统将取 96 分的颜色。

### 具体步骤
1.  修改 `src/pages/CalendarPage.tsx`。
2.  在 `dailyStats` `useMemo` 中，删除 `if (r.type === 'harvest') return;`。
3.  这样，`dailyStats` 将包含所有记录的统计数据。
4.  日历格子的颜色将自动根据包含“宝藏之旅”在内的所有记录计算得出。

我将立即执行此修改。