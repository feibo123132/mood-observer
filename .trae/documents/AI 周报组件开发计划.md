### 1. 恢复核心配置文件
**请您先手动操作**：根据上文指引，在源代码管理面板中恢复 `src/lib/cloudbase.ts`。
*(我将假设您在确认计划前已完成此操作，代码中将直接引用该文件)*

### 2. 创建 AI 周报组件 (`src/components/AIReportCard.tsx`)
*   **依赖**: 引用 `src/lib/cloudbase.ts` 中的 `app` 实例。
*   **UI**:
    *   **初始状态**: 漂亮的渐变色按钮 "✨ 生成本周 AI 深度情绪报告"。
    *   **加载状态**: 使用骨架屏 (Skeleton) 显示 "AI 正在阅读您的日记..."。
    *   **展示状态**: 模拟打字机效果逐字输出 DeepSeek 的分析报告。
*   **逻辑**: 包含完整的错误处理（如网络错误、无数据提示）。

### 3. 集成到周回顾页面 (`src/pages/ReviewPage.tsx`)
*   **修改**: 在 `ReviewPage` 组件中引入 `AIReportCard`。
*   **位置**: 放置在周导航条下方，排序按钮上方。
*   **交互**: 当用户切换不同周次时，自动重置 AI 分析组件的状态。