好的，我们严格按照您的要求分阶段进行。**第一阶段：SDK 集成与身份验证**。

本阶段我们将完成以下任务，且**绝不修改**现有的 `useMoodStore` 和情绪数据逻辑：

1.  **安装依赖**
    *   安装腾讯云开发官方 SDK: `@cloudbase/js-sdk`。

2.  **初始化云开发配置**
    *   创建 `src/lib/cloudbase.ts`。
    *   初始化 CloudBase 实例，并预留 `EnvID`（环境ID）的填空位置供您后续配置。

3.  **构建身份状态管理 (Auth Store)**
    *   创建 `src/store/useAuthStore.ts`。
    *   实现 `user` 状态管理，以及 `login`（登录）、`register`（注册）、`logout`（登出）和 `initAuth`（初始化检查）方法。

4.  **开发登录页面 (Login Page)**
    *   创建 `src/pages/LoginPage.tsx`。
    *   包含简洁的邮箱、密码输入框，以及登录/注册切换按钮。
    *   在 `src/App.tsx` 中注册 `/login` 路由。

5.  **添加首页用户入口**
    *   修改 `src/pages/HomePage.tsx`。
    *   在顶部导航栏增加**用户头像图标**。
    *   **交互逻辑**：
        *   未登录（灰色）：点击跳转至登录页。
        *   已登录（高亮）：点击弹出登出确认框。

确认计划后，我将立即开始代码实现。