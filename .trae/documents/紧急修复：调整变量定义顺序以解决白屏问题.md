非常抱歉！这是一个极其低级但致命的顺序错误。

### 🚨 事故现场分析
您截图中的报错信息非常明确：
`Uncaught ReferenceError: Cannot access 'activeAudioSrc' before initialization`

这发生在 `HomePage.tsx:52`。

**根本原因**：
我在代码中**先**使用了 `activeAudioSrc`（在 `useEffect` 中），**后**才定义它（在 `useMemo` 中）。
在 JavaScript/React 函数组件中，变量必须**先定义后使用**。虽然函数声明会被提升，但 `const` 定义的变量存在“暂时性死区 (TDZ)”，在执行到定义行之前访问它会直接抛出 ReferenceError，导致整个页面白屏崩溃。

### 🚑 紧急修复计划
我将立即调整 `HomePage.tsx` 中的代码顺序：

1.  **移动代码块**：将 `activeAudioSrc` 的 `useMemo` 定义块，移动到任何使用它的 `useEffect` **之前**。
2.  **规范顺序**：确保所有 Hook 的调用顺序符合依赖关系（State -> Ref -> Memo -> Effect）。

**修正后的顺序**：
1.  `useState` 定义
2.  `useRef` 定义
3.  `useMemo` 定义 (`activeAudioSrc`)
4.  `useEffect` 使用 (`audioRef.current.load()` 依赖 `activeAudioSrc`)

我将立刻执行这个修复，让您的电脑端恢复正常。再次为这个疏忽向您道歉！🙇‍♂️