我将为您实施“全屏可视化”的升级计划。这将从根本上改变用户体验，从一个局促的弹窗转变为一个沉浸式的独立页面。

### 实施计划

1.  **创建新页面：`ReportVisualizationPage.tsx`**
    *   在 `src/pages` 目录下创建一个全新的页面组件。
    *   这个页面将接收路由参数（URL Parameters）：`year` 和 `week`。
    *   它将包含一个全屏的布局容器，直接嵌入我们之前精心设计的 `ReportVisualization` 组件。
    *   添加一个优雅的“返回”按钮，允许用户随时回到历史报告列表。

2.  **注册路由：修改 `App.tsx`**
    *   添加一个新的路由路径：`/report-visualization/:year/:week`。
    *   将其指向新创建的 `ReportVisualizationPage`。

3.  **修改跳转逻辑：更新 `HistoryReportsPage.tsx`**
    *   移除原本用于控制弹窗的 `showVisualModal` 状态和相关的 Modal 代码。
    *   将侧边栏中“内容可视化”按钮的点击事件，从 `setShowVisualModal(true)` 改为使用 `navigate` 跳转到新页面。
    *   跳转链接格式：`/report-visualization/${selectedReport.year}/${selectedReport.week}`。

### 预期效果
用户点击“内容可视化”后，应用将进行页面跳转，进入一个宽敞、沉浸式的可视化分析页面。这不仅提供了更多的展示空间，也让之前设计的杂志级排版和动效能够得到最完美的呈现。

我将立即开始执行这个计划。